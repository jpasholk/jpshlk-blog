# Landing page absorbed the old About page(s).
# The ! forces the redirect even though Astro emits static meta-refresh
# pages at these paths (which would otherwise shadow the 301).
/about    /   301!
/about/   /   301!
/aboutme  /   301!
/aboutme/ /   301!
