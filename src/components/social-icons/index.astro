---
import Mail from './icons/Mail.astro'
import Github from './icons/Github.astro'
import Facebook from './icons/Facebook.astro'
import Youtube from './icons/Youtube.astro'
import Linkedin from './icons/Linkedin.astro'
import Twitter from './icons/Twitter.astro'
import Mastodon from './icons/Mastodon.astro'
import Link from './icons/Link-icon.astro'

const components = {
    mail: Mail,
    github: Github,
    facebook: Facebook,
    youtube: Youtube,
    linkedin: Linkedin,
    twitter: Twitter,
    mastodon: Mastodon,
    link: Link,
}

type Props = {
    kind: keyof typeof components
    href: string | undefined
    size?: number
    target: string | undefined
}

const {kind, href, size = 8, target} = Astro.props
const SocialSvg = components[kind]
---


<a class="text-sm text-gray-500 transition hover:text-gray-600" target={target} rel="noopener noreferrer" href={href}>
    <span class="sr-only">{kind}</span>
    <SocialSvg
            class={`fill-current text-gray-700 hover:text-primary-500 dark:text-gray-200 dark:hover:text-primary-400 h-${size} w-${size}`}/>
</a>
